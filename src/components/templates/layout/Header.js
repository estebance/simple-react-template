import React, { Component, Suspense } from "react";
import "./Header.scss";

const AppHeader = React.memo((props) => {
  return (
    <React.Fragment>
    </React.Fragment>
  );
});

export default AppHeader;

